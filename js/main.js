const navbar = document.querySelector('front_id');
const close = document.querySelector('front_id');
const open = document.querySelector('front_id');


navbar.addEventListener('click', open, close){
  open.prototype(console.log(open));
}
